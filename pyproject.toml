[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "andyjuan-personal-portfolio-radar"
version = "0.1.0"
description = "Personalized portfolio-aware market news radar."
readme = "README.md"
requires-python = ">=3.11"
authors = [
  { name = "AndyJuan Engineering" },
]
dependencies = [
  "agentmail",
  "beautifulsoup4",
  "feedparser",
  "httpx[http2]",
  "jinja2",
  "langid",
  "lxml",
  "openai",
  "premailer",
  "python-dotenv",
  "pyyaml",
  "rapidfuzz",
  # Pinned: the 11.0.213 -> 12.0.0 auth rewrite (Jul 2026) silently broke
  # CI overnight because this was unpinned. 13.x also removed the currency
  # rates endpoint (see src/portfolio/snaptrade_client._fx_rate_to_eur).
  # Bump deliberately and re-run the SnapTrade tests; the 13.x line moves fast.
  "snaptrade-python-sdk==13.0.13",
  "sqlite-utils",
  "structlog",
  "yfinance",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.0",
  "pytest-asyncio",
  "pytest-cov",
  "respx",
  "ruff",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
line-length = 88
target-version = "py311"

[tool.ruff.lint]
select = [
  "E",
  "F",
  "I",
]

[tool.setuptools.packages.find]
include = ["src*"]
